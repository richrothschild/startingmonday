{
  "version": 1,
  "description": "Luxury page sentinel rubric. Every route in the site inventory is scored against these dimensions on every run. A page passes only when all blocking dimensions pass.",
  "palette": {
    "requiredShellMarkers": [
      "bg-slate-950",
      "bg-slate-900"
    ],
    "bannedShellMarkers": [
      "bg-slate-100",
      "bg-slate-50",
      "bg-gray-50",
      "bg-gray-100",
      "bg-white/95 (opaque light card on dark shell)"
    ],
    "accentMarkers": [
      "orange-500",
      "orange-400",
      "orange-300",
      "orange-200"
    ]
  },
  "visualDiscipline": {
    "fontFamilies": {
      "countMonospaceAgainstLimit": false,
      "maxDistinctFamiliesByScope": {
        "funnel": 2,
        "dashboard": 2,
        "public": 2,
        "admin": 2
      }
    },
    "accentFamilies": {
      "trackedFamilies": [
        "orange",
        "amber",
        "yellow",
        "emerald",
        "green",
        "teal",
        "cyan",
        "blue",
        "indigo",
        "violet",
        "purple",
        "pink",
        "rose",
        "red"
      ],
      "maxDistinctFamiliesByScope": {
        "funnel": 2,
        "dashboard": 3,
        "public": 2,
        "admin": 4
      }
    }
  },
  "runtime": {
    "allowedStatusCodes": [200],
    "notFoundMarkers": [
      ">Page not found<",
      ">This page doesn't exist or has been moved<"
    ],
    "maxResponseMs": 8000
  },
  "coverage": {
    "description": "Every route in the inventory must receive an explicit verdict: pass, fail, or skip with an allowlisted reason. Any route without a verdict is a blocking coverage failure.",
    "allowedSkipReasons": [
      "dynamic-route-not-sampled",
      "runtime-skipped-by-flag"
    ]
  },
  "dimensions": [
    {
      "id": "availability",
      "blocking": true,
      "metric": "HTTP status is 200 and body does not contain 404 markers"
    },
    {
      "id": "palette-conformance",
      "blocking": true,
      "metric": "Page source uses the dark luxury shell (required markers present at shell level, banned light-shell markers absent)"
    },
    {
      "id": "dead-link-hygiene",
      "blocking": true,
      "metric": "Internal links on the page resolve to routes that exist in the route inventory"
    },
    {
      "id": "latency",
      "blocking": false,
      "metric": "Response time under maxResponseMs"
    },
    {
      "id": "typography-discipline",
      "blocking": false,
      "metric": "Route stays within allowed distinct font-family count for its scope"
    },
    {
      "id": "accent-restraint",
      "blocking": false,
      "metric": "Route stays within allowed distinct accent-color family count for its scope"
    },
    {
      "id": "coverage",
      "blocking": true,
      "metric": "Every inventoried route has a verdict (pass/fail) or an allowlisted skip reason; unexplained gaps fail the run"
    }
  ],
  "reporting": {
    "channel": "slack",
    "cadence": "hourly",
    "escalation": "Any blocking dimension failure produces a Slack alert naming the exact route, dimension, and evidence"
  }
}
