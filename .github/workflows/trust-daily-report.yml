name: Trust Daily Report

on:
  schedule:
    - cron: '40 13 * * *'
  workflow_dispatch:

permissions:
  actions: read
  contents: read

jobs:
  trust-daily-report:
    name: Build trust daily report
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install chromium --with-deps

      - name: Refresh trust integrity snapshot for evidence snippets
        env:
          PLAYWRIGHT_BASE_URL: https://startingmonday.app
          PLAYWRIGHT_TEST_EMAIL: ${{ secrets.PLAYWRIGHT_TEST_EMAIL }}
          PLAYWRIGHT_TEST_PASSWORD: ${{ secrets.PLAYWRIGHT_TEST_PASSWORD }}
          SLACK_UI_DELIVERY_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          SLACK_UI_DELIVERY_CHANNEL: reliability---service
        run: |
          node scripts/trust-integrity-agent.mjs \
            --base-url "$PLAYWRIGHT_BASE_URL" \
            --report docs/status/trust-integrity.latest.json || true

      - name: Generate trust daily report
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SLACK_RELIABILITY_SERVICE_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          RELIABILITY_SLACK_CHANNEL: reliability---service
          TRUST_INTEGRITY_REPORT_PATH: docs/status/trust-integrity.latest.json
        run: node scripts/trust-daily-report.mjs

      - name: Upload trust daily report artifacts
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: trust-daily-latest
          path: |
            docs/status/trust-daily.latest.json
            docs/status/trust-daily.latest.md
            docs/status/trust-integrity.latest.json
            docs/status/trust-integrity.latest.md
          retention-days: 14

      - name: Alert when trust daily report fails
        if: failure()
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          if [ -z "$SLACK_WEBHOOK_URL" ]; then
            echo "No Slack webhook configured; skipping failure alert."
            exit 0
          fi
          RUN_URL="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          curl -X POST -H 'Content-type: application/json' --data '{
            "text": "🚨 Trust daily report failed",
            "blocks": [
              {
                "type": "section",
                "text": {
                  "type": "mrkdwn",
                  "text": "*Trust daily report failed*\nChannel: reliability---service\n<'"$RUN_URL"'|View workflow run>"
                }
              }
            ]
          }' "$SLACK_WEBHOOK_URL"
