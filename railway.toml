[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
startCommand = "echo '=== STARTUP ===' && node --version && echo 'PORT='$PORT && ls .next/BUILD_ID && npm run start -- --port $PORT"
healthcheckPath = "/api/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
