import { logger } from '../lib/logger.js'

// Implemented in WBS 2.5 — Contact Tracker follow-up reminders
export async function runFollowupJob() {
  logger.info('followup-job: not yet implemented (WBS 2.5)')
}
